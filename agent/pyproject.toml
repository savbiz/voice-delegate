[project]
name = "voice-delegate-agent"
version = "0.2.0.dev1"
requires-python = ">=3.12"
license = "Apache-2.0"
license-files = ["LICENSE"]
readme = "README.md"
dependencies = ["langgraph>=1.2.2", "langchain-openai>=1.6.2"]
[build-system]
requires = ["uv_build>=0.12.17,<0.13"]
build-backend = "uv_build"
