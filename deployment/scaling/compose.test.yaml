services:
  api-a:
    command: ["/app/.venv/bin/uvicorn", "voice_delegate.scaling.test_app:create_test_app", "--factory", "--host", "0.0.0.0", "--port", "8000", "--workers", "1", "--no-access-log"]
  api-b:
    command: ["/app/.venv/bin/uvicorn", "voice_delegate.scaling.test_app:create_test_app", "--factory", "--host", "0.0.0.0", "--port", "8000", "--workers", "1", "--no-access-log"]
