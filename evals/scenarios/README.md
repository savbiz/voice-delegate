# Scripted control scenarios

`control.json` supplies synthetic provider decisions to `evals/test_scenarios.py`.
These cases check scheduling, cancellation and bounded execution without model calls.
They contain no recordings and do not measure real speech or model quality.
The separate versioned worker dataset exercises retrieval, arithmetic and adversarial context.
